# Variables #
SRC := ./src
# La convención de variables es mayúsculas además, hay que tener cuidado al comentar al final de una línea porque se puede agregar espacios vacíos#
BUILD := ./build
DEBUG := ./debug

C_FILES := $(wildcard *.c)

%: %.o
	mkdir -p ${BUILD}/bin
	gcc ${BUILD}/%<.o -o ${BUILD}/bin/%@
%.o: ${SRC}/%.c
	mkdir -p ${BUILD}
	gcc -c ${SRC}/*.c -o ${BUILD}/%@.o
clean:
	rm -fr ${BUILD}