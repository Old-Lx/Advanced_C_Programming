#include <stdio.h>

#include "pointers.h"


my_number = 7; // Un int
my_number_ptr = &my_number; // Un apuntador a un int, el rvalue & indica el segmento de memoria donde se aloja una variable
// my_number_ptr = *my_number; Esto es análogo a la línea de arriba, pero el operador * es más versátil y permite múltiples dereferencias
struc_ptr_example;